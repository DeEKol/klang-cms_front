/*
 * Компонент, футер
 */
// ? Slice Imports
import styles from "./Footer.module.css";

export function Footer() {
    return (
        <footer className={styles.Footer}>
            <span>klang cms</span>
            <span>v1.0.0</span>
        </footer>
    );
}
